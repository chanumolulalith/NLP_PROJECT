# ── Core requirements (always install these) ──────────────────────────────
streamlit
pandas
numpy
matplotlib
scikit-learn
openpyxl

# ── Spell correction and basic NLP helpers ─────────────────────────────────
textblob

# ── Zero-shot classification and extractive QA (HuggingFace) ──────────────
transformers
sentencepiece

# ── Optional: gensim for true Word2Vec ────────────────────────────────────
# gensim requires C++ build tools to compile and does NOT support Python 3.13+
# The app has a built-in sklearn fallback — Word2Vec works WITHOUT gensim.
# Only uncomment if you are on Python 3.9 – 3.12 and have Visual C++ installed.
# gensim

# ── Optional: TensorFlow for the Deep Learning tab ────────────────────────
# TensorFlow only supports Python 3.9 – 3.12.
# The app runs fully without it — the Deep Learning tab shows a friendly message.
# Only uncomment if you are on Python 3.9 – 3.12.
# tensorflow
